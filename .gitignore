# Saídas geradas pelo pipeline (não versionar o conteúdo das pastas)
data/*
# ...exceto o CSV real de Paulo Afonso (fonte DATASUS, versionado de propósito)
!data/pauloafonso.csv
figures/*
# ...exceto as figuras principais exibidas no README (versionadas de propósito)
!figures/previsao_hmpa.png
!figures/pauloafonso_atendimentos.png

# Artefatos do Julia
*.jl.cov
*.jl.mem
/doc/
/build/
deps/deps.jl

# Ambiente local (geralmente não versionado em projetos de pacote)
/.julia/

# SO / editores
.DS_Store
*.swp
