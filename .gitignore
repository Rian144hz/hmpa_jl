# Saídas geradas pelo pipeline (não versionar o conteúdo das pastas)
data/*
figures/*
# ...exceto a figura principal exibida no README (versionada de propósito)
!figures/previsao_hmpa.png

# Artefatos do Julia
*.jl.cov
*.jl.mem
/doc/
/build/
deps/deps.jl

# Ambiente local (geralmente não versionado em projetos de pacote)
/.julia/

# SO / editores
.DS_Store
*.swp
